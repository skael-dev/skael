---
import Base from '../layouts/Base.astro';
---

<Base title="skael — AI agent skill control plane">
  <main>
    <h1>skael</h1>
  </main>
</Base>

<style>
  main {
    max-width: var(--max);
    margin: 0 auto;
    padding: 4rem 1.5rem;
  }

  h1 {
    font-size: clamp(2.5rem, 6vw, 5rem);
    font-weight: 700;
    letter-spacing: -0.03em;
    color: var(--text);
  }
</style>
